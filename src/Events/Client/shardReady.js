/**
 * @param {import("../Struct/Client")} client
 */

// biarin aja, buat ngecek shard id saat shard ready
export default async (shardId) => {
  console.log(`GianMasbro Spawnned Shard Id - ${shardId}`);
};
